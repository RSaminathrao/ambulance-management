{
  "name": "ambulance-management",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dispatch:worker --prefix server\" \"npm run dev --prefix client\"",
    "dev:core": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:with-dispatch": "npm run dev",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.0.1"
  }
}
